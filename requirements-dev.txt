# Development-only dependencies.
# `mendeleev` is used solely by scripts/generate_elements.py to (re)generate
# app/static/data/elements.json. It is NOT needed to run the web app.
-r requirements.txt
mendeleev==1.3.0
pytest==8.4.2
